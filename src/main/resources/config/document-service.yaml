server:
  port: 1991

eureka:
  instance:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/documentsDb

management:
  tracing:
    sampling:
      probability: 1.0